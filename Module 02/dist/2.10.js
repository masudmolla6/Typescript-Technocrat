{
    // Mapped Type
    const arrayOfNumbers = [1, 2, 3, 4];
    const arrayOfString = arrayOfNumbers.map((num) => {
        return num;
    });
    const area1 = {
        height: "100",
        width: 50,
    };
    // 
}
export {};
